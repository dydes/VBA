Sub isOpen()
Dim i As Integer
For i = 1 To Workbooks.Count
    If Workbooks(i).Name = "员工花名册.xlsx" Then
        MsgBox "文件已打开！"
        Exit Sub
    End If
Next
MsgBox "文件没有打开！"
End Sub
'需要注意的是，文件必须用excel打开，wps不行
